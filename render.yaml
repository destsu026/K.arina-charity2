services:
  - type: web
    name: arina-charity
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: EASYDONATE_BASE_URL
        sync: false
      - key: EASYDONATE_API_KEY
        sync: false
