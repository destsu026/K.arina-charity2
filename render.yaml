services:
  - type: web
    name: easydonate-dashboard
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: EASYDONATE_BASE_URL
        sync: false
      - key: EASYDONATE_API_KEY
        sync: false
